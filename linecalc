#where line_vec is what you get from doing the point slope cleverness
line_vec

# Define the point in your 2D plane that you want to find the closest point to
p = np.array([x, y])

# Find the projection of p onto the line
t = np.dot(p - p1, line_vec) / np.dot(line_vec, line_vec)
projection = p1 + t * line_vec

# The point on the line that is closest to p is the projection point
closest_point_on_line = projection
